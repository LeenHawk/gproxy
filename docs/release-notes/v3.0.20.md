## v3.0.20

> Update Codex CLI compatibility and plugin request authentication metadata.
> 更新 Codex CLI 兼容性及插件请求认证元数据。

### English

#### Fixed

- Preserve `OAI-Product-Sku` through Codex plugin service forwarding. Plugin catalog and workspace sharing requests default to `codex` when the client supplies no product selection.

#### Changed

- Update the default Codex CLI identity and model discovery client version from `0.153.2` to `0.156.0`, retaining client-supplied identity values.

### 简体中文

#### 修复

- Codex 插件服务转发时保留 `OAI-Product-Sku`。插件目录和工作区共享请求未指定产品时，默认使用 `codex`。

#### 变更

- 默认 Codex CLI 身份及模型发现客户端版本从 `0.153.2` 更新至 `0.156.0`，保留客户端提供的身份字段。
